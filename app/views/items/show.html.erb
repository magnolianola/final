<p>
<b><%= @item.name %></b>: 
<%= @item.description %>
<p>

<% if current_user %>
    <%= link_to "Delete this Item", {:user_id => @user.id, :id => @item.id}, :confirm => "Are you sure you want to delete this item?", :method => :delete %>
  <% end %>

  <p><%= image_tag( @item.image.url ) %></p>